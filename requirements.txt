# Runtime dependency for the ingestion tasks.
# In docker this is installed via _PIP_ADDITIONAL_REQUIREMENTS (see .env / docker-compose).
# Listed here so the DAG also runs in a plain (non-docker) Airflow environment.
requests>=2.31,<3
apache-airflow~=3.2.0
pandas>=2.2,<3
scikit-learn>=1.5,<2
joblib>=1.4,<2
lxml
# ML modeling stack for the copd_train_validate_test DAG.
catboost>=1.2,<2
xgboost>=2.1,<3
lightgbm>=4.0,<5
mlflow>=2.14,<3
numpy>=1.26,<2
